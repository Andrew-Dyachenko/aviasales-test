// import {
// 	fetchTickets } from '../../helpers/fetch'

// const dispatch = jest.fn()

describe('Fetch', () => {
	describe('Tickets', () => {
		it('Should fetch and dispatch tickets data', () => {
			// const url = '../../../public/tickets.json'
			// const preFetch = fetchTickets(url)
			// const result = preFetch(dispatch)
		})
	})
})